import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    dimension: {
        width: '100%',
        height: '100%',
    },
    footer: {
        position: 'absolute',
        bottom: 10,
    },
});

export default styles;